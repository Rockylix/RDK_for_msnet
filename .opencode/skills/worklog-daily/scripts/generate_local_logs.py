#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
生成本地日志文件(daily/*.md 和 Worklog.md)
"""

from datetime import datetime
from pathlib import Path
import json

def generate_capture_daily_log():
    """生成 Capture 项目的每日日志 (2026-02-01)"""
    content = """# 2026-02-01 工作日志

## 📋 项目：RDK双摄像头实时显示

### 摘要
实现了基于RDK开发板的双摄像头实时图像采集与SSH X11转发显示功能。通过硬件VIO接口配置MIPI摄像头,集成OpenCV进行图像处理,最终实现640×480分辨率的双路实时显示。

### 详细技术路径

#### 1. VIO摄像头配置与图像采集 (commit: 9d73031)
- **配置参数**: 
  - 分辨率: 1920×1080
  - 位深: 16bit
- **文件**: `capture/capture_dis.cpp`, `capture/Makefile`
- **成果**: 成功采集并保存测试图像

#### 2. OpenCV集成与SSH X11转发实时显示
- **实现方案**:
  - 将C代码改为C++,在Makefile中链接OpenCV4库
  - 使用`sp_vio_get_yuv()`获取NV12格式图像
  - 通过`cvtColor()`转换为BGR格式
  - 使用`imshow()`实现SSH X11转发显示

#### 3. VPS硬件编码尝试 ❌
- **目标**: 利用板载VPS进行编码和缩放,减少CPU占用
- **问题**: 
  - 使用VPS处理后`get_frame`失败
  - 两张图片分辨率相同导致VPS流ID冲突
  - ISP处理完成但数据无法到达VPS进行缩放
- **决策**: 放弃VPS方案,改用Sensor直接降分辨率

#### 4. Sensor降分辨率方案 ✅
- **实现**:
  - 修改sensor配置,直接输出640×480图像
  - 取消OpenCV的resize操作
- **性能测试**:
  - 关闭imshow: FPS = 30 (达到摄像头极限帧率)
  - 开启SSH X11转发: FPS = 4~5
  - YUV→BGR转换: 对帧率影响极小

#### 5. 最终解决方案
- **双路输出策略**:
  - 第一路: 640×480 (用于后续处理,待缩放)
  - 第二路: 320×240 (用于SSH显示)
- **性能**: 保持30FPS稳定输出

### 参数记录

| 指标 | 数值 |
|------|------|
| 摄像头分辨率 | 1920×1080 → 640×480 |
| 显示分辨率 | 320×240 |
| 极限帧率 | 30 FPS |
| SSH转发帧率 | 4~5 FPS |
| YUV转BGR耗时 | 忽略不计 |

### 遗留问题

1. **SSH X11转发性能瓶颈**: 远程显示占用大量CPU资源,严重影响帧率
2. **VPS硬件加速未实现**: 流ID冲突问题未解决,无法利用硬件加速

### Git 提交记录
- `9d730316` - modules (初始提交,包含主要代码)
- `a8a84743` - Rename LOG.md to README.md
"""
    
    return content

def generate_msnet_daily_log():
    """生成 MSNet 项目的汇总日志 (2026-01-11 ~ 2026-01-26)"""
    content = """# 2026-01-26 工作日志 (汇总)

## 📋 项目：MSNet立体视觉深度估计

### 时间跨度
2026-01-11 ~ 2026-01-26 (共16天开发周期)

### 摘要
实现了基于RDK开发板BPU的MSNet立体视觉深度估计模型推理。解决了NFS文件共享、模型加载错误、量化校准数据冲突等关键技术问题,最终成功实现板载实时推理,推理延迟36.93ms。

### 详细技术路径

#### 阶段1: 开发环境配置 (1.11)
**目标**: 建立Windows ↔ ARM开发板双向文件共享

- **NFS服务器配置** (开发板侧):
  - 安装`nfs-kernel-server`
  - 配置共享目录`/home/root/workspace`
  - 开放端口: 2049, 111, 1098

- **Remote-SSH配置**:
  - 配置`~/.ssh/config`实现快速连接
  - VS Code Remote-SSH扩展实时编辑板载文件

- **Windows NFS客户端**:
  - 使用haneWIN NFS Server共享本地目录
  - 开发板挂载: `mount -t nfs -o nolock <server_ip>:/msnet <path>`

**Git**: `6e21eedc`, `21a42286`

#### 阶段2: 模型加载问题排查 (1.13) 🔴
**问题**: BPU_loadModel返回错误码-6000002

**排查过程**:
1. 初步怀疑模型过大 (错误模型33.75MB vs 正常模型90MB)
2. 检查Docker环境,发现原始模型应为304MB
3. **根因**: NFS传输过程中中断,导致文件不完整

**解决方案**:
- ✅ 确保文件传输完整性
- ✅ 降低模型分辨率 640×352→384×192

**性能对比**:
| 模型分辨率 | 文件大小 | 加载时间 |
|-----------|---------|---------|
| 684×384 | 304MB | 21.04s |
| 384×192 | - | 6.59s |

**Git**: `c41edb5f`, `01a45571`

#### 阶段3: 内存管理问题 (1.14) 🔴
**问题**: 扩展Swap导致内存占满

**影响**:
- NTP服务崩溃 → 系统时间错误 → Makefile误检测
- 编译失败,无法进行代码编辑

**Git**: `ae519223`

#### 阶段4: 推理结果异常 (1.20 ~ 1.26) ⚡
**现象**: 输入不同图像,输出结果相同(中间蓝,周围红的无意义噪点)

**排查过程**:
1. ✅ 验证ONNX模型正常
2. ✅ 确认BPU工作且利用率高
3. ✅ 验证内存读写正常
4. **定位三重致命冲突** 🎯

**三重致命冲突分析**:

##### 冲突1: 数值量程灾难 (最严重)
- **脚本逻辑**: `log_np / 255.0` → 缩放至[0,1]
- **Config配置**: `mean_value: 123.675` + `data_mean_and_scale`
- **后果**: 输入值 = ([0~1] - 123.675) × 0.017 ≈ -2.1
- **结果**: 信号淹没在死区,网络无法提取纹理

##### 冲突2: 内存排布冲突
- **脚本逻辑**: `transpose(2,0,1)` → CHW排布
- **Config配置**: `input_layout_rt: 'NHWC'`
- **后果**: 量化工具按NHWC解析CHW数据 → 三色重叠

##### 冲突3: 颜色通道冲突
- **脚本逻辑**: `convert('RGB')`
- **Config配置**: `input_type_rt: 'bgr'`
- **后果**: RGB/BGR通道顺序错位

**解决方案**:
1. ✅ 取消校准数据的`/255`归一化
2. ✅ 确保校准数据为uint8格式(非float)
3. ✅ 开启`preproccess_on: True`由模型内部处理
4. ✅ 统一使用BGR格式
5. ✅ 排除letterbox影响,仅使用cut方法

**验证结果**:
- 使用官方模型DStereoV2.6_int8.bin → ✅ 推理成功
- 使用自定义msnet模型(684×384) → ✅ 推理成功

**Git**: `e5a47985` (最终版本)

### 参数记录

#### 推理性能 (官方模型 640×352)
| 阶段 | 耗时 |
|------|------|
| 加载模型 | 262.11 ms |
| GetModelInfo | 0.48 ms |
| 预处理 | 97.04 ms |
| **推理** | **36.93 ms** |
| 后处理 | 63.57 ms |
| **总计** | ~460 ms |

#### 模型规格对比
| 模型 | 分辨率 | 文件大小 | 加载时间 |
|------|--------|---------|---------|
| 官方 | 640×352 | - | 262ms |
| 自定义1 | 684×384 | 304MB | 21.04s |
| 自定义2 | 384×192 | - | 6.59s |

### 关键经验总结 💡

1. **NFS文件传输陷阱**: 
   - 传输大文件(>100MB)必须等待完成,否则生成不完整文件
   - 建议: 传输后校验MD5或文件大小

2. **量化校准数据黄金法则**:
   - 校准数据格式必须与config配置严格一致
   - 数据类型: uint8 (禁止float)
   - 内存排布: 与`input_layout_rt`一致
   - 颜色空间: 与`input_type_rt`一致
   - 归一化: 交给模型内部处理(`preproccess_on: True`)

3. **Swap内存扩展风险**:
   - 过度扩展会导致系统服务崩溃(NTP, systemd)
   - 建议: 优化代码内存占用,而非盲目扩展Swap

### 遗留问题

1. **MSNet自定义模型推理质量**: 虽然能跑通,但效果待验证
2. **模型加载时间优化**: 384×192模型6.5s仍较慢

### Git 提交记录
- `1f1f98a8` - modules (初始日志)
- `6e21eedc`, `21a42286` - NFS配置
- `c41edb5f`, `01a45571`, `ae519223` - 模型加载问题
- `e5a47985` - 量化校准问题最终解决
"""
    
    return content

def generate_mipi_msnet_daily_log():
    """生成 MIPI MSNet 项目的每日日志 (2026-02-03)"""
    content = """# 2026-02-03 工作日志

## 📋 项目：MIPI双目视觉实时深度估计

### 摘要
实现了从MIPI双目摄像头到视差图的完整流程。通过双线程采集优化、曝光白平衡同步尝试,将系统FPS从4提升至8(无SSH转发)。

### 详细技术路径

#### 1. 基础流程打通 ✅
**实现**: MIPI摄像头 → YUV采集 → BPU推理 → 视差图生成 → SSH显示

**性能测试**:
- 开启SSH转发显示视差图: FPS = 4
- 关闭SSH转发: FPS = 8

**结论**: 输入图像采集不占用太多资源,瓶颈在SSH转发

**Git**: commit包含`mipi_msnet/cpp/main.cpp`

#### 2. 双线程时间戳同步 ⚡
**目标**: 解决双目图像不同步导致的视差图边缘破碎问题

**原始代码**:
```cpp
sp_vio_get_frame(_camera1, (char*)sys_mem.virAddr, ...);
sp_vio_get_frame(_camera2, (char*)sys_mem.virAddr+_infer_size, ...);
```

**优化方案**:
- 使用`std::thread`并行获取两路图像
- 记录时间戳,计算帧间时间差
- 若时间差>5ms,重试(最多5次)

**结果**: 
- ❌ 边缘破碎感并无明显改善
- **结论**: 软件时间戳对齐不是主要问题,需要硬件级AE/AWB同步

#### 3. 曝光与白平衡同步尝试 🔄
**问题**: Camera1明显比Camera2亮,色温不一致

**分析**:
- RDK的hbn框架占用摄像头设备
- 无法直接使用`v4l2-ctl`命令设置参数
- 需要在hbn框架内部实现AE/AWB锁定

**待实现方案**:
1. 锁定曝光参数: 手动设置相同曝光时间和增益
2. 色温锁定: 使用固定AWB模式(如"日光"模式)

**状态**: 未完成 (文档截止)

#### 4. 未完成功能
- ❌ 双目标定与图像校正 (需要内参/外参矩阵)

### 参数记录

| 指标 | 数值 |
|------|------|
| SSH转发FPS | 4 |
| 无转发FPS | 8 |
| 时间戳同步阈值 | 5ms |
| 最大重试次数 | 5次 |

### 遗留问题

1. **硬件级AE/AWB同步未实现**: 
   - 需要研究hbn框架的传感器控制API
   - 目标: 实现两路摄像头参数锁定

2. **双目标定缺失**:
   - 需要标定板采集数据
   - 计算内参/外参矩阵
   - 实现图像校正算法

3. **双线程优化效果有限**:
   - 软件时间戳对齐无明显改善
   - 需要硬件触发同步或主从模式

### Git 提交记录
- `9d730316` - modules (包含mipi_msnet完整代码)
"""
    
    return content

def generate_worklog_md():
    """生成 Worklog.md 主日志索引"""
    content = """# RDK开发板项目工作日志

> 本文档记录RDK开发板相关项目的开发日志索引

---

## 📅 2026年2月

### [2026-02-03](./daily/2026-02-03.md) - MIPI双目视觉实时深度估计
**项目**: mipi_msnet  
**分类**: 硬件/算法  
**今日成果**: 
- ✅ 完成MIPI双目摄像头→视差图完整流程
- ✅ 双线程时间戳同步实现(软件对齐)
- 🔄 曝光白平衡同步方案调研

**关键指标**: FPS 8 (无SSH转发), 时间戳同步<5ms  
**遗留问题**: AE/AWB硬件同步, 双目标定未完成

---

### [2026-02-01](./daily/2026-02-01.md) - RDK双摄像头实时显示
**项目**: capture  
**分类**: 硬件  
**今日成果**:
- ✅ VIO摄像头配置与图像采集(1920×1080)
- ✅ OpenCV集成与SSH X11转发实时显示
- ❌ VPS硬件编码尝试失败(流ID冲突)
- ✅ Sensor降分辨率方案(640×480 + 320×240双路输出)

**关键指标**: 极限帧率30FPS, SSH转发4~5FPS  
**遗留问题**: SSH X11转发性能瓶颈, VPS硬件加速未实现

---

## 📅 2026年1月

### [2026-01-26](./daily/2026-01-26.md) - MSNet立体视觉深度估计 (最终总结)
**项目**: msnet  
**分类**: 算法  
**时间跨度**: 2026-01-11 ~ 2026-01-26  
**今日成果**:
- ✅ 成功定位并解决量化校准数据三重致命冲突
- ✅ 官方模型推理验证通过(推理延迟36.93ms)
- ✅ 自定义模型(384×192, 684×384)推理成功

**关键发现** 🎯:
1. **数值量程灾难**: 校准数据/255 + Config减均值 → 信号淹没
2. **内存排布冲突**: CHW vs NHWC → 三色重叠
3. **颜色通道冲突**: RGB vs BGR → 通道错位

**性能指标**: 推理36.93ms, 预处理97.04ms, 后处理63.57ms  
**遗留问题**: 模型加载时间较长(6.5s), 推理质量待验证

**关键经验**:
- NFS大文件传输必须完整(>100MB)
- 量化校准数据必须与Config严格一致(类型/排布/色彩)
- Swap过度扩展导致系统服务崩溃

---

## 📊 项目统计

| 项目 | 时间跨度 | 关键成果 | 核心问题 |
|------|---------|---------|---------|
| capture | 2.1 | 双摄像头30FPS实时显示 | SSH转发瓶颈, VPS硬件加速失败 |
| msnet | 1.11~1.26 | BPU推理36.93ms | 量化校准冲突, NFS文件损坏 |
| mipi_msnet | 2.3 | 双目深度估计8FPS | AE/AWB同步, 双目标定缺失 |

---

## 🔗 相关资源

- [Capture项目目录](./capture/)
- [MSNet项目目录](./msnet/)
- [MIPI MSNet项目目录](./mipi_msnet/)
"""
    
    return content

def main():
    """主函数:生成所有本地日志文件"""
    base_path = Path(r'E:\embedded AI\RDK_board_pro')
    daily_path = base_path / 'daily'
    
    # 确保 daily 目录存在
    daily_path.mkdir(exist_ok=True)
    
    # 生成每日日志
    logs = {
        '2026-02-01.md': generate_capture_daily_log(),
        '2026-01-26.md': generate_msnet_daily_log(),
        '2026-02-03.md': generate_mipi_msnet_daily_log()
    }
    
    for filename, content in logs.items():
        file_path = daily_path / filename
        with open(file_path, 'w', encoding='utf-8') as f:
            f.write(content)
        print(f"✅ 生成: {file_path}")
    
    # 生成 Worklog.md
    worklog_path = base_path / 'Worklog.md'
    with open(worklog_path, 'w', encoding='utf-8') as f:
        f.write(generate_worklog_md())
    print(f"✅ 生成: {worklog_path}")
    
    print(f"\n🎉 所有本地日志文件生成完成!")
    print(f"   - 每日日志: {len(logs)} 个文件")
    print(f"   - 主索引: Worklog.md")

if __name__ == '__main__':
    main()
